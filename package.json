{
  "name": "vader",
  "version": "0.0.0-pre-mvp",
  "description": "Vader compiler and standard library",
  "type": "module",
  "module": "src/index.ts",
  "scripts": {
    "vader": "bun src/index.ts",
    "test": "bun test",
    "test:update": "bun test --update-snapshots",
    "build": "bun build src/index.ts --target=bun --outdir=dist",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.5.0"
  },
  "trustedDependencies": []
}
