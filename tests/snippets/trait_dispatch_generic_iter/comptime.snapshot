# Comptime

## tests/snippets/trait_dispatch_generic_iter

## generic instances
  Repeat<i32>
  Repeat<T>
  Repeat<T>
  Once<i32>
  Once<T>
  Once<T>
  Into<Target>
  Add<Rhs, Out>
  Sub<Rhs, Out>
  Mul<Rhs, Out>
  Div<Rhs, Out>
  Rem<Rhs, Out>
  Index<I, T>
  ArrayOps<T>
  IndexSet<I, T>
  Contains<T>
  Iterator<i32>
  Iterator<T>
  Iterator<T>
  Yield<i32>
  Yield<T>
  Yield<T>
  Yield<T>
  Yield<T>
  Yield<T>
  Async<T>
  Range<i32>
  Range<usize>
  Range<T>
  Range<T>
  Range<T>
  ArrayIterator<T>
  ArrayIterator<T>
  ArrayIterator<T>

# Diagnostics
[26:9] warning[W0014] mutating a parameter that carries no `!` — parameters are becoming read-only borrows ; add `!` to the parameter name to keep mutating it: `remaining` is reached through an immutable path
[39:9] warning[W0014] mutating a parameter that carries no `!` — parameters are becoming read-only borrows ; add `!` to the parameter name to keep mutating it: `yielded` is reached through an immutable path
