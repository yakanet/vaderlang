// Mixing `-> T` with `= expr` is rejected at parse time (P1020). The two
// forms are mutually exclusive: `= expr` infers; `-> T { ... }` annotates.

export square :: fn(x: i32) -> i32 = x * x

main :: fn() -> i32 {
    return 0
}
