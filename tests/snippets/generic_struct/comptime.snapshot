# Comptime

## tests/snippets/generic_struct

## generic instances
  Stack<i32>
  Stack<T>
  Stack<T>
  Stackable<T>
  Into<Target>
  Add<Rhs, Out>
  ArrayOps<T>
  Sub<Rhs, Out>
  Mul<Rhs, Out>
  Div<Rhs, Out>
  Rem<Rhs, Out>
  Index<I, T>
  IndexSet<I, T>
  Contains<T>
  Iterator<T>
  Iterator<T>
  Yield<T>
  Yield<T>
  Yield<T>
  Async<T>
  Range<i32>
  Range<usize>
  Range<T>
  Range<T>
  Range<T>
  ArrayIterator<T>
  ArrayIterator<T>
  ArrayIterator<T>

# Diagnostics
[19:9] warning[W0014] mutating a parameter that carries no `!` — parameters are becoming read-only borrows ; add `!` to the parameter name to keep mutating it: `push` mutates its receiver (declared `self!`), but this receiver is immutable — annotate `self!` at tests/snippets/generic_struct/_main.vader:18:16
[20:9] warning[W0014] mutating a parameter that carries no `!` — parameters are becoming read-only borrows ; add `!` to the parameter name to keep mutating it: `size` is reached through an immutable path — annotate `self!` at tests/snippets/generic_struct/_main.vader:18:16
[23:9] warning[W0014] mutating a parameter that carries no `!` — parameters are becoming read-only borrows ; add `!` to the parameter name to keep mutating it: `size` is reached through an immutable path — annotate `self!` at tests/snippets/generic_struct/_main.vader:22:15
