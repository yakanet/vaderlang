export classify :: fn(x: i32 | string | null) -> string {
    return match x {
        is i32 if x > 0 -> "positive"
        is i32 -> "non-positive"
        is string -> x
        is null -> "nothing"
        _ -> "?"
    }
}
