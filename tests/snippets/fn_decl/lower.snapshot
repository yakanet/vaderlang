# Lower

## snippet
  fn snippet$add(a: i32, b: i32) -> i32
    return
      add :i32
        l:
          a :i32
        r:
          b :i32
  fn snippet$greet(name: string, prefix: string) -> string
    return prefix :string
  fn snippet$host_log(level: i32, msg: string) -> void
    <extern / signature only>
