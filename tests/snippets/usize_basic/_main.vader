module "snippet"

import "std/io"

export add :: fn(a: usize, b: usize) -> usize {
    return a + b
}

main :: fn() -> i32 {
    n: usize = 10
    m: usize = add(n, 32)
    println("${m}")
    return 0
}
