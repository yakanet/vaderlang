# vader CLI failed (exit 134)
vader: panic — midir/emit: no field `count` on `__genstate_filter<char>` in 'snippet$count_ls' — a method reference on a primitive isn't a first-class value ; call it directly or wrap it in a lambda
