# Comptime

## tests/snippets/namespace_enum_variant

## tests/snippets/namespace_enum_variant/palette

## generic instances
  __genstate_array_iter<T>
  Into<Target>
  Add<Rhs, Out>
  Sub<Rhs, Out>
  Mul<Rhs, Out>
  Div<Rhs, Out>
  Rem<Rhs, Out>
  Index<I, T>
  ArrayOps<T>
  IndexSet<I, T>
  Contains<T>
  Iterator<T>
  Iterator<T>
  Iterator<T>
  Async<T>
  Range<i32>
  Range<usize>
  Range<T>
  Range<T>
  Range<T>

# Diagnostics
[19:13] warning[W0016] the enum name adds nothing here — the slot already pins it, so the bare variant reads the same: write `.Red`
[20:13] warning[W0016] the enum name adds nothing here — the slot already pins it, so the bare variant reads the same: write `.Green`
[26:29] warning[W0016] the enum name adds nothing here — the slot already pins it, so the bare variant reads the same: write `.Blue`
