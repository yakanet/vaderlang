// Backing-type / bounds / duplicate-value diagnostics.
export TooBig :: enum(u8) {
    A
    B = 300       // out of u8 range
}

export DupVal :: enum {
    X = 1
    Y = 2
    Z = 1         // duplicate
}

export NotInt :: enum(string) {
    A
}
