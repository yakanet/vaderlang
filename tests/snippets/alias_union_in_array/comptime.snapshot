# Comptime

## tests/snippets/alias_union_in_array

## tests/snippets/alias_union_in_array/types

## generic instances
  Into<Target>
  Add<Rhs, Out>
  ArrayOps<T>
  Sub<Rhs, Out>
  Mul<Rhs, Out>
  Div<Rhs, Out>
  Rem<Rhs, Out>
  Index<I, T>
  IndexSet<I, T>
  Contains<T>
  Iterator<T>
  Iterator<T>
  Yield<T>
  Yield<T>
  Yield<T>
  Async<T>
  Range<i32>
  Range<usize>
  Range<T>
  Range<T>
  Range<T>
  ArrayIterator<T>
  ArrayIterator<T>
  ArrayIterator<T>

# Diagnostics
[18:5] warning[W0014] mutating a parameter that carries no `!` — parameters are becoming read-only borrows ; add `!` to the parameter name to keep mutating it: `push` mutates its receiver (declared `self!`), but this receiver is immutable — annotate `arr!` at tests/snippets/alias_union_in_array/_main.vader:17:16
