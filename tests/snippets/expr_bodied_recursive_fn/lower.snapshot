# Lower

## snippet
  fn snippet$fact(n: i32) -> ?
    ~>
      if :?
        cond:
          lte :bool
            l:
              n :i32
            r:
              int(1) :i32
        then:
          ~>
            int(1) :i32
        else:
          ~>
            mul :?
              l:
                n :i32
              r:
                call :?
                  callee:
                    fact :fn(i32) -> ?
                  arg0:
                    sub :i32
                      l:
                        n :i32
                      r:
                        int(1) :i32
  fn snippet$main() -> i32
    return
      call :?
        callee:
          fact :fn(i32) -> ?
        arg0:
          int(5) :i32

# Diagnostics
[8:8] error[T3034] recursive expression-bodied function requires an explicit `-> Type` annotation: `fact`
