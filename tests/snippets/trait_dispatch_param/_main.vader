// Trait-method dispatch on a bounded type param: `fn[T: Hash](x: T)`.
// `x.hash()` resolves at mono time to the concrete type's `hash` impl.

export hash_twice :: fn[T: Hash](x: T) -> u64 {
    return x.hash() + x.hash()
}

main :: fn() -> i32 {
    n :: hash_twice(7)
    return i32(n)
}
