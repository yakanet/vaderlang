# Comptime

## tests/snippets/std_base64/_main.vader

## generic instances
  assert_eq(i32)
  assert_eq(isize)
  assert_eq(string)
  assert_eq(usize)
  Iterator(char)
  Yield(char)
  Yield(i32)
  Yield(string)
  Yield(u8)
  Yield(usize)
  Range(char)
  Range(i32)
  Range(usize)
  ArrayIterator(string)
