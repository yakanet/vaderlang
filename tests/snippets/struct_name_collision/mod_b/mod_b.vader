// Module B : its `Box` carries a string `label`. Same name as
// `mod_a::Box`, different shape — that's the whole point of the
// regression test.
module "snippet/mod_b"

export Box :: struct {
    label: string
}

export make_b :: fn() -> Box {
    return Box { .label = "hello" }
}
