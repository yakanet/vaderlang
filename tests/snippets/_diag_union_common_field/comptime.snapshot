# Comptime

## tests/snippets/_diag_union_common_field

## generic instances
  __genstate_array_iter<T>
  Into<Target>
  Add<Rhs, Out>
  Sub<Rhs, Out>
  Mul<Rhs, Out>
  Div<Rhs, Out>
  Rem<Rhs, Out>
  Index<I, T>
  IndexSet<I, T>
  Contains<T>
  Iterator<T>
  Iterator<T>
  Iterator<T>
  Async<T>
  Range<i32>
  Range<usize>
  Range<T>
  Range<T>
  Range<T>
  ArrayOps<T>

# Diagnostics
[24:45] error[T3008] expression is not indexable: `.label` sits at a different position in each variant of `Back | Front` (`Back` at 0, `Front` at 1) — narrow with `if`/`match` first
