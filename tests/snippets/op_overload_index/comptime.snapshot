# Comptime

## tests/snippets/op_overload_index

## generic instances
  ArrayOps<T>
  Into<Target>
  Add<Rhs, Out>
  Sub<Rhs, Out>
  Mul<Rhs, Out>
  Div<Rhs, Out>
  Rem<Rhs, Out>
  Index<I, T>
  IndexSet<I, T>
  Contains<T>
  Iterator<T>
  Iterator<T>
  Yield<T>
  Yield<T>
  Yield<T>
  Async<T>
  Range<i32>
  Range<usize>
  Range<T>
  Range<T>
  Range<T>
  ArrayIterator<T>
  ArrayIterator<T>
  ArrayIterator<T>

# Diagnostics
[29:17] warning[W0014] mutating a parameter that carries no `!` — parameters are becoming read-only borrows ; add `!` to the parameter name to keep mutating it: this element is reached through an immutable path
[34:9] warning[W0014] mutating a parameter that carries no `!` — parameters are becoming read-only borrows ; add `!` to the parameter name to keep mutating it: `push` mutates its receiver (declared `self!`), but this receiver is immutable
[35:9] warning[W0014] mutating a parameter that carries no `!` — parameters are becoming read-only borrows ; add `!` to the parameter name to keep mutating it: `push` mutates its receiver (declared `self!`), but this receiver is immutable
