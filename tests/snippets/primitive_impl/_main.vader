import "std/io" { println }

i32 implements Display {
    fn show(self) -> string {
        return "the answer is ${self}"
    }
}

main :: fn() -> i32 {
    x: i32 = 42
    println(x.show())
    return 0
}
