# Comptime

## tests/snippets/custom_iter

## generic instances
  ArrayOps<T>
  Into<Target>
  Add<Rhs, Out>
  Sub<Rhs, Out>
  Mul<Rhs, Out>
  Div<Rhs, Out>
  Rem<Rhs, Out>
  Index<I, T>
  IndexSet<I, T>
  Contains<T>
  Iterator<T>
  Iterator<T>
  Yield<i32>
  Yield<T>
  Yield<T>
  Yield<T>
  Async<T>
  Range<i32>
  Range<usize>
  Range<T>
  Range<T>
  Range<T>
  ArrayIterator<T>
  ArrayIterator<T>
  ArrayIterator<T>

# Diagnostics
[16:9] warning[W0014] mutating a parameter that carries no `!` — parameters are becoming read-only borrows ; add `!` to the parameter name to keep mutating it: `current` is reached through an immutable path — annotate `self!` at tests/snippets/custom_iter/_main.vader:11:16
