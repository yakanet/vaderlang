apply :: fn(f: fn(i32) -> i32, x: i32) -> i32 {
    return f(x)
}

main :: fn() -> i32 {
    bias := 100
    biased :: fn(x: i32) -> i32 {
        return x + bias
    }
    return apply(biased, 23)
}
