// Code after a divergent statement (`return`) — TS emits W0002
// (unreachable code) as a non-fatal warning. Vader's codes.vader
// doesn't define W0002 yet (only W0001), so the warning stays
// TS-only until the port catches up.
main :: fn() -> i32 {
    return 0
    x :: 42
    return x
}
