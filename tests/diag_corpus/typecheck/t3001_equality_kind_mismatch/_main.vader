module "snippet"

// `==` compares scalars by representation. `Handle` is a `CPointer`, so it
// cannot equal an `isize` — convert one side explicitly (`isize(h)`).
Handle :: CPointer

INVALID_HANDLE: isize: -1

is_invalid :: fn(h: Handle) -> bool = h == INVALID_HANDLE

main :: fn() -> i32 = 0
