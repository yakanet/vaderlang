// A width `c_struct_layout` has no alignment rule for: it uses the width as the
// field's alignment, and `at % 3` describes no ABI.
module "snippet"

@c_struct("struct timeval")
Timeval :: struct {
    @c_size(3, .Linux)
    tv_sec: i64
}

main :: fn() -> i32 = 0
