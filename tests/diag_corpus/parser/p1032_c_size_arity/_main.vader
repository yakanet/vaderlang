// A lone width does not say which system it describes, so both arguments are
// required — P1032.
module "snippet"

@c_struct("struct timeval")
Timeval :: struct {
    @c_size(4)
    tv_sec: i64
}

main :: fn() -> i32 = 0
