import "std/io" { println }

fib1 :: fn(n: i32) -> i32 {
     a := 0
     b := 1
     for n > 0 {
         c :: a + b
         a = b
         b = c
         n = n - 1
     }
     return a
}

/*
// TODO §1.5b iterators
fib2 :: fn(n: i32) -> i32 {
     a := 0
     b := 1
     for 0..<n {
         c :: a + b
         a = b
         b = c
     }
     return a
}
*/

main :: fn() -> i32 {
    println("Fib1: ${fib1(50)}")
    // println("Fib2: ${fib2(50)}")
    return 0
}