{
  "name": "@vaderlang/lsp-server",
  "scripts": {
    "compile": "bun build --format=cjs --targer=node --sourcemap=linked --packages=external --outdir=out src/index.ts",
    "watch": "bun build --format=cjs --targer=node --sourcemap=linked --packages=external --watch --outdir=out src/index.ts"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "dependencies": {
		"vscode-languageserver": "^9.0.1",
		"vscode-languageserver-textdocument": "^1.0.12"
	},
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}