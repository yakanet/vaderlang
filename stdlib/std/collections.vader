// std/collections — generic collections.
//
// Kotlin convention: each collection has a read-only and a mutable version.
// MutableX is a subtype of the read-only X.

// Sequence collections.
List         :: struct(T: type) { /* ... */ }
MutableList  :: struct(T: type) { /* ... */ }

// Key/value collections.
Map          :: struct(K: type, V: type) where K: Hash + Eq { /* ... */ }
MutableMap   :: struct(K: type, V: type) where K: Hash + Eq { /* ... */ }

// Set collections.
Set          :: struct(T: type) where T: Hash + Eq { /* ... */ }
MutableSet   :: struct(T: type) where T: Hash + Eq { /* ... */ }

// TODO: full implementations once the compiler supports the generic + trait
// machinery required.
